import 'package:flutter/material.dart';

import 'components/body.dart';

class PersonelLogin extends StatelessWidget {
  const PersonelLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
